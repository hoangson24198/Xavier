<resources>

    <style name="AppTheme.Fullscreen" parent="AppTheme">
        <item name="android:actionBarStyle">@style/Widget.AppTheme.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.Xaler.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/colorPrimary</item>
        <item name="fullscreenTextColor">@color/colorAccent</item>
    </style>
</resources>